let contador = 0;
// Formas de exibir mensagens na tela:

let limite = prompt("Digite até quando contar:");
alert("Sua mensagem");

while( contador <=limite){
    console.log(contador);
    contador++;
}

console.log("CONTADOR:"+ contador);
